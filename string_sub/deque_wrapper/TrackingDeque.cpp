#include "TrackingDeque.h"

// template <typename T>
// TrackingDeque<T>::TrackingDeque() : max_size(0) { 
//   // std::cout << "constructor\n"; 
//   // using std::deque<T>::std::deque<T>;
// }

// template <typename T>
// TrackingDeque<T>::~TrackingDeque() {
//   std::cout << "---> " << max_size << "<---" << std::endl;
// }

// template <typename T>
// void TrackingDeque<T>::test_function(const T& random) {
//   this->push_front(random);
//   std::cout << random << std::endl;
// }

// template <typename T>
// void TrackingDeque<T>::push_front(const T& val) {
  // this->push_front(val);
  // (max_size < this->size) ? max_size = this->size : max_size = max_size;
  // std::cout << this->size << std::endl;
  // (max_size < size()) ? max_size = size() : max_size = max_size;
// }

// template <typename T>
// void TrackingDeque<T>::push_front(T&& val) {
//   this->push_front(val);
// }

// template <typename T>
// void TrackingDeque<T>::push_back(const T& val) {
  // this->push_back(val);
// }

// template <typename T>
// void TrackingDeque<T>::push_back(T&& val) {
//   this->push_back(val);
// }